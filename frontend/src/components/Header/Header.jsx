const Header = () => {
    return (
        <header className="header">
            <h2>This software is under heavy development, be adviced that everything can still change!</h2>
        </header>
    );
};

export default Header;